name: flutter_application_1
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=2.19.6 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_map: ^1.0.0
  flutter_osm_plugin: ^0.53.4+1
  mapbox_flutter: ^1.0.0-dev.1.1
  udp: ^5.0.3
  mapsforge_flutter: ^2.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets : 
  - assets/arrow.png

flutter_icons:
  image_path: "assets/logo.png"
  map_path: "assets/map_openstreet.osm"
  android: true
  ios: true
